{
  "name": "usalamasms",
  "version": "0.1.0",
  "private": true,
  "license": "SEE LICENSE IN LICENSE",
  "type": "module",
  "description": "UsalamaSMS — aviation safety management for operators the incumbents priced out. Offline-first, Annex 19 Amendment 2 native.",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev": "vite --config apps/web/vite.config.js",
    "check:assets": "node scripts/check-assets.mjs",
    "check:brand": "node scripts/check-brand.mjs",
    "check:claims": "node scripts/check-claims.mjs",
    "check:css": "node scripts/check-css.mjs",
    "check:glyphs": "node scripts/check-glyphs.mjs",
    "check:prose": "node scripts/check-prose.mjs",
    "check": "npm run prisma:generate && npm run typecheck && npm run check:brand && npm run check:assets && npm run check:claims && npm run check:css && npm run check:glyphs && npm run check:prose && npm run check:retraction && npm run check:wiring && npm run check:switches && npm run test",
    "test": "vitest run",
    "test:watch": "vitest",
    "build": "npm run check && npm run icons && vite build --config apps/web/vite.config.js && npm run stamp-sw",
    "prisma:generate": "prisma generate",
    "prisma:migrate": "prisma migrate dev",
    "typecheck": "tsc --noEmit",
    "icons": "node scripts/build-icons.mjs",
    "brand": "node scripts/derive-brand.mjs",
    "stamp-sw": "node scripts/stamp-sw.mjs",
    "smoke": "node scripts/smoke.mjs",
    "verify": "npm run build && npm run smoke && npm run check:a11y && npm run check:update",
    "test:integration": "vitest run --config vitest.integration.config.ts",
    "db:up": "bash scripts/local-db.sh",
    "setup:env": "node scripts/setup-env.mjs",
    "seed": "node scripts/seed.mjs",
    "check:update": "node scripts/check-update.mjs",
    "seed:demo": "node scripts/seed-demo.mjs",
    "check:wiring": "node scripts/check-wiring.mjs",
    "migrate:deploy": "prisma migrate deploy",
    "check:a11y": "node scripts/check-a11y.mjs",
    "check:switches": "node scripts/check-switches.mjs",
    "check:retraction": "node scripts/check-retraction.mjs",
    "og-card": "node scripts/make-og-card.mjs"
  },
  "devDependencies": {
    "@netlify/functions": "^5.3.0",
    "@types/jsonwebtoken": "^9.0.7",
    "@types/node": "^22.9.0",
    "axe-core": "^4.13.0",
    "playwright": "^1.56.1",
    "prisma": "^7.0.0",
    "typescript": "^5.6.3",
    "vite": "^5.4.10",
    "vitest": "^2.1.4"
  },
  "dependencies": {
    "@fastify/rate-limit": "^11.2.0",
    "@prisma/adapter-pg": "^7.9.1",
    "@prisma/client": "^7.0.0",
    "argon2": "^0.41.1",
    "dexie": "^4.0.10",
    "fastify": "^5.1.0",
    "jsonwebtoken": "^9.0.2",
    "pg": "^8.23.0",
    "zod": "^3.23.8"
  }
}
